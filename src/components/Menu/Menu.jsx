import React from 'react';
import SectionTitle from '../../shared/SectionTitle/SectionTitle';

const Menu = () => {
    return (
        <div className='my-4'>
            <SectionTitle subtitle={'Check it out'}/>
        </div>
    );
};

export default Menu;