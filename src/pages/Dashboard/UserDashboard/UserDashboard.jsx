import React from 'react';
import { Helmet } from 'react-helmet-async';

const UserDashboard = () => {
    return (
        <div>
            <Helmet>
            <title> Welcome to User Dashboard | Bistro Boss</title>
        </Helmet>
            this is user Dashboard
        </div>
    );
};

export default UserDashboard;